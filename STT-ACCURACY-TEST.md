# 8kHz 오디오 체인 정렬 후 STT 정확도 측정

> 측정일: 2026-05-08
> 변경 커밋: `c438ce1` (TTS 송출 체인을 8kHz 단일 레이트로 정렬)
> 환경: 로컬 callbot (extension 2001) ← Asterisk 172.31.79.202 ← 브라우저 (extension 1003, ws://...:8088/ws)
> STT: Google Cloud Speech V2, model=`telephony`, language=`ko-KR`, 16kHz (callbot 이 8k→16k 업샘플)

## 목적

웹 입력 텍스트 → Google TTS → WebRTC → Asterisk → callbot STT 경로의 인식률을 검증.
8kHz 단일 레이트 정렬 변경(`c438ce1`)이 끝난 시점에서 SK 쉴더스 사이버보안 FAQ
(`skshieldus_cyber_faq.md`) 에서 추출한 10개 질의로 측정.

## 측정 방법

- 통화 1회 동안 스케줄러로 11개 audio 자동 송신
- 송신 간격: 30초 (callbot 의 STT 윈도우 ~15s + LLM 응답 ~10-15s 가정)
- 첫 발화는 priming 용 "안녕" (warmup, 결과 집계 제외)
- 콜봇 측 `[STT] Final:` 로그를 입력 텍스트와 매칭

## 결과 (10개 질의)

| # | 입력 (sent text) | STT 결과 (transcript) | 일치율 | 판정 |
|---|------------------|----------------------|--------|------|
| 1 | 파견보안관제와 원격관제의 차이는 무엇인가요 | 화면 보안보화원벽관 / 의 차이는 무엇인가요 (split) | ~30% | ❌ FAIL |
| 2 | 중소기업도 인증이 꼭 필요한가요 | 중소기업도 인증이 꼭 필요한가요 | 100% | ✅ PASS |
| 3 | ISMS-P 인증을 받으면 무엇이 달라지나요 | 아이스 핀을 받으면 무엇이 달라진다요 | ~55% | ❌ FAIL |
| 4 | 관리체계 구축은 어디서부터 시작해야 하나요 | 관리책이 구축은 어디서부터 시작해야 하나예요 | ~82% | △ PASS (loose) |
| 5 | 제로트러스트 보안이란 무엇인가요 | 재료 트라티보안입니다 | ~30% | ❌ FAIL |
| 6 | 침해사고 분석 서비스가 왜 필요한가요 | 치매 사고 분석 서비스가 왜 필요한가요 | ~84% | △ PASS (loose) |
| 7 | 랜섬웨어는 어떻게 유입되나요 | 랜섬메라는 어떤 | ~25% | ❌ FAIL |
| 8 | 보안 운영 서비스는 왜 필요한가요 | 보안영 서비스는 왜 필요한가요 | ~78% | △ PASS (loose) |
| 9 | OT보안 컨설팅 범위산정은 어떻게 하나요 | 볼티포항 컨설팅터지 않나요 | ~30% | ❌ FAIL |
| 10 | 얼굴인식기는 실외에도 설치 가능한가요 | 얼굴 인식기는 시대에서 설치 가능한가요 | ~80% | △ PASS (loose) |

## 정확도 집계

| 기준 | PASS / 10 | 정확도 |
|------|-----------|--------|
| 엄격 (완전 일치) | 1 | **10%** |
| 느슨 (≥78% char 일치, 의도 유지) | 5 | **50%** |

**목표 80% 미달.**

## 오인식 패턴 분석

대부분의 실패가 **도메인 특수어** 에서 발생.

| 도메인어 | STT 결과 |
|---------|---------|
| 파견 | 화면 / 판변 |
| 보안관제 | 보안단지 / 보화원벽관 |
| ISMS-P | 아이스 핀 |
| 제로트러스트 | 재료 트라티 |
| 침해 | 치매 |
| 랜섬웨어 | 랜섬메라 |
| OT보안 | 볼티포항 |
| 운영 | 영 (드롭) |
| 실외 | 시대 |

일반 어휘는 정상 인식:
- "인증", "분석", "서비스", "필요한가요", "어디서부터", "시작해야", "설치 가능한가요" 등
- Q2 "중소기업도 인증이 꼭 필요한가요" 는 일반어만으로 구성되어 100% 일치

이전 24kHz 체인에서는 이런 일반어조차 흔히 오인식됐던 점에 비하면 **음질 자체는 개선** 됐음.
잔존 오인식은 주로 **STT 모델이 도메인 어휘를 모르는** 한계.

## 추가 관측

### Q1 split

`파견보안관제와 원격관제의 차이는 무엇인가요` (30+자, ~6초 오디오) 는 두 turn 에 걸쳐
분할됨 — Turn 1 STT 윈도우(~15s) 가 timeout 되면서 앞부분만 캡쳐, 뒷부분은 다음 Turn 에서.

원인: TTS WAV 의 길이가 turn 윈도우 + 송신 간격 정합과 어긋남. 30초 간격이 짧아 long
audio 가 흘러넘침.

### Q5, Q9 의 큰 오인식

`재료 트라티`, `볼티포항` 같이 음소가 크게 어긋난 케이스는 callbot 의 RTP 패킷 도달이
불안정한 구간(콜봇 로그의 `Done: N/M reads had audio` 비율이 낮음) 에서 발생. WebSocket
→ UDP 변환 과정의 timing/jitter 영향으로 추정.

## 결론

`c438ce1` 의 8kHz 체인 정렬은 **음질 자체는 개선** (이전엔 일반어도 오인식되던 게 정상화).
다만 80% 목표를 달성하려면 추가 작업 필요:

- **A) STT phrase hints (Speech Adaptation API)** — `callbot/stt.py` 의 `RecognitionConfig`
  에 도메인 어휘 boost. 가장 직접적. FAQ 어휘를 화이트리스트로 등록.
- **B) STT 모델 변경 시도** — `telephony` → `chirp_2` 또는 `latest_long`. 1줄 변경으로
  Korean accuracy 비교.
- **C) 송신 간격 / 오디오 청크 처리 개선** — 30초 → 40초+ 로 늘리거나 long audio 를
  silence padding 으로 분할.

이번 plan(`zoiper-expressive-flame.md`) 의 범위는 web-rtc 만이었으므로, 추가 작업은 별도
plan 에서 결정.

## 측정 원본 로그 위치

- 전체 callbot 로그: `/tmp/callbot.log` (라인 774 이후가 본 측정 구간, 5콜 수행 / 마지막 5콜이 본 측정용)
- 11개 STT result: 위 로그에서 `grep "STT result:"` 추출 (14건 중 non_voice 3건은 측정 사이의 idle window)
